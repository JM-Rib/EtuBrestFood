const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "etubrestfood",
      password: "etubrestfood",
      database: "etubrestfood",
    },
    listPerPage: 10,
};
module.exports = config;